Function expressions in JavaScript are anonymous functions assigned to variables.

```js
const greet = function(name) {
  return `Hello, ${name}!`;
};

console.log(greet('John')); // Output: Hello, John!

```